# Next.js Frontend Environment Variables
# =====================================

# API Backend URL (used by middleware for CSP configuration)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Additional allowed origins for production CSP (comma-separated)
# Example: https://api.example.com,https://cdn.example.com
NEXT_PUBLIC_ALLOWED_ORIGINS=

# API request timeout in milliseconds
NEXT_PUBLIC_API_TIMEOUT_MS=15000

# Build & Development Tools
# -------------------------
# Enable bundle analyzer (set to "true" to analyze bundle)
ANALYZE=false

# Enable Turbopack mode (Next.js experimental bundler)
TURBOPACK=false
